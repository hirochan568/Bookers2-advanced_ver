<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>

         <% @user.followers.each do |user| %>
        <table>
        <tr>
          <td>  <%= user.name %> </td>
          <td><%= link_to '詳細', user_path(user) %></td>
        </tr>

        </table>
        <% end %>

     </div>
  </div>
</div>