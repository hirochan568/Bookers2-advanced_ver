

<h3>Books search for "<%= params[:words] %>"</h3>


<table class='table table-hover table-inverse'>
  <thead>
    <tr><% if @range == "User" %>
      <th></th>
      <th>user</th>
      <th>introduction</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
     <% @users.each do |user| %>
      <tr>
        <td><%= link_to user_path(user) do %>
          <%= attachment_image_tag(user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
          <% end %>
        </td>
        <td><%= user.name %></td>
        <td><%= user.introduction %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% else %>
<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th>Favorite</th>
      <th>Comment</th>
      <th colspan="5"></th>
    </tr>
  </thead>
  <tbody>
     <% @books.each do |book| %>
      <tr>
        <td><%= link_to user_path(book.user_id) do %>
          <%= attachment_image_tag(book.user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
          <% end %>
          <%= link_to book.user.name, book_path(book), class: "book_#{book.id}" %>
        </td>
        <td><%= link_to book.title, book_path(book), class: "book_#{book.id}" %></td>
        <td><%= book.body %></td>
        <td> <div id="likes_buttons_<%= book.id %>">
            <%= render 'favorites/favorite-btn', book: book %></div></td>
        <td>コメント件数：<%= book.book_comments.count %></td>
      </tr>
    <% end %> <% end %>
  </tbody>
</table>
